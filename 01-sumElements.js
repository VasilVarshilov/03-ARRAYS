function solve(arr) {
    let result = +arr[0] + +arr[arr.length - 1];
    console.log(result);
    
}
solve(["2", "3", "8"]);


// s "+" pravim array-a chislo -   +arr[0] + +arr[arr,length - 1]
